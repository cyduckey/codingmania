
### Keyword Plans
introduce a keyword named `instead` which works for:
    (a) to navigate when error happens {hard and browser stopping errors}, so that the developer and the user does not stuck at error.
    example: encountered (err) ? instead do this (…);
                

introducing a keyword named `allow` which shortens the logics for authentications and verifications.\

introduce another keyword named `diary` for:
    (a)